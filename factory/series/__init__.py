"""Seasons of connected episodes: cast, levels, standings, copy, long-form.

Contracts that generators may import: `outcome`, `trace`, `story`, `cast`.
Everything else here reads outcomes and traces and never imports a generator
(it reaches one only through `generators.get`). See docs/08-series-layer.md.
"""
